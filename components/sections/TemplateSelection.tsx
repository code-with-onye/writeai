import React from "react";
import Breadcrumb from "../ui/Breadcrumb";
import { templateSelection } from "../../data";

const TemplateSelection = () => {
  return <Breadcrumb items={templateSelection} />;
};

export default TemplateSelection;
